{
  "name": "shenai-sdk-example-js",
  "version": "2.5.0",
  "private": false,
  "dependencies": {
    "serve": "^14.2.4"
  },
  "scripts": {
    "build": "rm -rf build && mkdir build && cp -r src/* ./shenai-sdk build/",
    "serve": "serve -s build --config=../serve.json",
    "start": "yarn build && yarn serve"
  }
}